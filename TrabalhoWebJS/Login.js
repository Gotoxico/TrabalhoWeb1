function validar_login(username, password, validator){
    let user = username.value
    let senha = password.value
    let validacao = validator.value

    if(user=="admin" && senha=="admin" && validacao == "")

}